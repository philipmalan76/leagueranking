
namespace LeagueRanking
{
    /// <summary>
    /// Enum to keep the ranking rules
    /// </summary>
    public enum Rule: int
    {
        Win = 3,
        Draw = 1,
        Lose = 0,
    }
}
